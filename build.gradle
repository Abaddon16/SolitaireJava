plugins {
    id('java')
    id('application')
}

group = 'com.abaddon16'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.9.1')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

application{
    mainClass = 'com.abaddon16.Driver'
}

test {
    useJUnitPlatform()
}

tasks.register("buildAndRun"){
    dependsOn build
    dependsOn run
    doLast{
        println("Gradle finished gracefully")
    }
}